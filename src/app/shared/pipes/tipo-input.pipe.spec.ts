import { TipoInputPipe } from './tipo-input.pipe';

describe('TipoInputPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoInputPipe();
    expect(pipe).toBeTruthy();
  });
});
